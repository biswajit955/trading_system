# ═══════════════════════════════════════════════════════════════
# NSE Algo Trading Bot — Python 3.12
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ── Core ─────────────────────────────────────────────────────────
yfinance==1.2.0          # NSE 5-min OHLCV data — DO NOT upgrade
                         # v2.x changes column structure and breaks fetch
pandas>=2.1.0,<3.0.0    # DataFrame operations
pytz>=2024.1             # IST timezone (Asia/Kolkata)

# ── yfinance backend (auto-installed with yfinance) ──────────────
curl-cffi>=0.7.0         # HTTP engine used by yfinance internally
requests>=2.31.0         # fallback HTTP client
multitasking>=0.0.11     # parallel data fetching
peewee>=3.17.0           # yfinance local cache